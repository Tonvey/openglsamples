cmake_minimum_required(VERSION 2.8)
project(StudyOpenGL)
IF(APPLE)
   #INCLUDE_DIRECTORIES ( /System/Library/Frameworks )
   FIND_LIBRARY(COCOA_LIBRARY Cocoa)
   FIND_LIBRARY(GLUT_LIBRARY GLUT )
   FIND_LIBRARY(OpenGL_LIBRARY OpenGL )
   FIND_LIBRARY(GLEW_LIBRARY OpenGL )
   MARK_AS_ADVANCED (COCOA_LIBRARY GLUT_LIBRARY OpenGL_LIBRARY GLEW_LIBRARY)
   SET(EXTRA_LIBS ${COCOA_LIBRARY} ${GLUT_LIBRARY} ${OpenGL_LIBRARY} ${GLEW_LIBRARY})
ENDIF (APPLE)

file(GLOB PROJECT_SRCS ./src/main.c)
include_directories(./include/)

#find_package(OpenGL REQUIRED)
#find_package(GLUT REQUIRED)
#find_library(COCOA_LIB cocoa)
#include_directories( ${OPENGL_INCLUDE_DIR}  ${GLUT_INCLUDE_DIR})
add_definitions(-DGL_SILENCE_DEPRECATION)
add_subdirectory(./01_triangle)
add_subdirectory(./02_circle)
add_subdirectory(./03_star)
add_subdirectory(./04_sin_cos)
add_subdirectory(./05_polygon)
